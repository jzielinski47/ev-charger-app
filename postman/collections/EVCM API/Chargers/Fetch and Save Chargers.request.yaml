$kind: http-request
name: Fetch and Save Chargers
method: POST
url: "{{baseUrl}}/api/chargers/ocm/save"
description: Fetches EV charger points in proximity and persists them to the database (lat=50.0, lon=20.0, radius=10km, max=100).
order: 2000
